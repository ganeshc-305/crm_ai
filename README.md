# CRM AI Assistant (Prototype)

Prototype for the AI Launchpad fresher training project. Implements a Streamlit UI, multi-agent RAG pipeline, and PostgreSQL integration.

Next steps:
- Install deps from requirements.txt
- Implement vector ingestion and retrieval
- Build supervisor and agent modules in src/agents
- Wire PostgreSQL for chat history
- Add tests and deployment docs
